spring.config.use-legacy-processing: true
server:
    port: 1000

spring:
    h2:
        console:
            enabled: true
            path: /h2-console
    datasource:
        url: jdbc:h2:file:./data/sample
        # driverClassName: jdbc:h2:file:/data/sample
        username: sa
        # password: 1234
    jpa:
        hibernate:
            ddl-auto: create
# logging:
#     file:
        # 
        # path: ./logs

---

spring:
    config:
        activate:
            on-profile: "dev"
    profiles:
        include:
        - file-logging
        - message-logging
    h2:
        console:
            enabled: true
            path: /h2-console
    datasource:
        url: jdbc:h2:file:./data/dev
        username: sa
        password: 1234
    jpa:
        hibernate:
            ddl-auto: create
